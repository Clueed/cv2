# jobs

Job sourcing/discovery module. Goal: surface new postings that match Mark's target criteria (e.g. GTM Strategy & Ops, founding GTM, DACH-based AI/SaaS companies) from job boards or other sources, so the [[../resume/]] + cv-customizer skill can be pointed at them.

Stack and pipeline shape are not yet decided — see the parent `AGENTS.md`.

## Out of scope (handled elsewhere)

- Tailoring the CV to a specific JD → `.agents/skills/cv-customizer/`
- Application pipeline tracking → not built; revisit after sourcing works
- Cover letter drafting → not built
